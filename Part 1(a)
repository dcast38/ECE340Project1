// Code your design here
module one_bit_full_adder(A, B, Cin, S,
Cout);
  
input A, B, Cin;
output reg S, Cout;

  always @(*) begin
      S = Cin ^ (A ^ B);
      Cout = A&B|Cin&(A^B);
      end
    endmodule
